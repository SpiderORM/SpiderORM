name: Publish Python Package

on:
  push:
    tags:
      - v*.*.*  # Isso será acionado quando uma tag que começa com "v" seguida por números, como v1.0.0, for criada
    branches:
      - main 
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.12.2 # Substitua pela versão do Python que você deseja usar

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install setuptools wheel twine

    - name: Build the package
      run: python setup.py sdist bdist_wheel

    - name: Publish to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: pypi-AgEIcHlwaS5vcmcCJGI1NzRjMGQ4LWNjZDYtNDVlOS1iZDkyLWFhNjE0NDk0Y2QzZAACKlszLCI4ZDBlMzJhNC1lZjRhLTRiNjEtYTczOC1iMTJmNmZhNTAyZmEiXQAABiCn7_NQLcG6CHi5yruBC9C2IIPuAF62_HFClNshCEUoZg
      
      run: |
        echo ${{ secrets.PY_PI_PASSWORD }}
        twine upload --verbose -r pypi dist/*

    - name: Clean up build artifacts
      run: rm -rf build dist *.egg-info
